# mini-base site

Static export of the mini-base website and documentation.

## Vercel deployment

Import this repository into Vercel with these settings:

- Framework preset: `Other`
- Build command: leave empty
- Install command: leave empty
- Output directory: `build`

The deployable website is in `build/`. The original Next.js source is kept private and is not included in this repository.
